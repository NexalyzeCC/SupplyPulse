[functions]
  directory = "netlify/functions"

[[scheduled_functions]]
  function = "scheduled-scan"
  schedule = "0 6 * * *"

[dev]
  command = "npm run start"
  port = 8888
  targetPort = 3001

[build]
  command = "npm run build"
  publish = "build"
